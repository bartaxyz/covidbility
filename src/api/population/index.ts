import { getCountryPopulation } from "./getCountryPopulation";
import { getGlobalPopulation } from "./getGlobalPopulation";

export const population = {
  getCountryPopulation,
  getGlobalPopulation
};
