import { getCurrent } from "./getCurrent";
import { getCurrentLocation } from "./getCurrentLocation";
import { getHistoric } from "./getHistoric";

export const corona = {
  getCurrent,
  getCurrentLocation,
  getHistoric
};
